// @arclight/client-core — 端共享层（reducer / SSE 重连 / discovery）
// slice1（Unit 2）落 EventStreamManager / SessionReducer / EpochTracker；本文件为包占位入口。
export const CLIENT_CORE_PLACEHOLDER = "slice1" as const;
