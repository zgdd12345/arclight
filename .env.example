# arclight 配置（优先级：process.env > .arclight/config.json > ~/.config/arclightagent/config.json > 内置默认）
# 必填：缺失则 serve 启动即失败（slice0 起即强制；slice2 接 provider 后真正使用）
ANTHROPIC_API_KEY=

# 可选
# ARCLIGHT_PORT=43127
# ARCLIGHT_HOST=127.0.0.1
