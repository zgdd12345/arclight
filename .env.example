# arclight 配置（优先级：process.env > .arclight/config.json > ~/.config/arclightagent/config.json > 默认）
# Provider 协议 = Anthropic Messages（D4）。二选一：

# 方式 A：Anthropic 官方
# ANTHROPIC_API_KEY=sk-ant-...

# 方式 B：智谱 GLM（Anthropic 兼容端点，D4 补充记账——成本优先，当前默认部署方式）
# ZHIPU_API_KEY=...
# 自动取 https://open.bigmodel.cn/api/anthropic/v1 + glm-4.6；可用 ARCLIGHT_MODEL / ANTHROPIC_BASE_URL 覆盖

# 可选
# ARCLIGHT_PORT=43127
# ARCLIGHT_HOST=127.0.0.1
# ARCLIGHT_MODEL=glm-4.6
